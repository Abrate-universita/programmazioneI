#include <stdio.h>

int main(void) {
    float f1, f2;
    printf("Inserisci due numeri reali: ");
    scanf("%f %d", &f1, &f2); // %d -> %f

    float risultato = (f1 + f2) / 2); // la parentesi iniziale
    printf("Risultato = %f %d\n", &risultato); // %d non ci dev'essere e togli & dal risultato

    // Calcoliamo il quadrato
    // float quadrato = risultato * risultato; // ma è un commento scemo
    printf("Il quadrato e' \n", quadrato) } // } -> orribile, manca %f e ;
}

